'use client';

import BoutiqueLoading from '../../[domain]/loading';

/**
 * @fileOverview Reuses the Boutique identity loader for direct UID links.
 */
export default function StoreLoading() {
  return <BoutiqueLoading />;
}
